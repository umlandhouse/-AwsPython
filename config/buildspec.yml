version: 0.1
phases:
  install:
    commands:
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket rob-python-lambda --output-template-file output.yaml
artifacts:
  type: zip
  files:
    - samTemplate.yaml

